# 青甘川高原自驾路书

一个以地图为核心的静态自驾路书 MVP。

## 本地运行

```powershell
npm install
npm run dev
```

浏览器打开终端显示的本地地址。

## 构建

```powershell
npm run build
npm run preview
```

构建产物位于 `dist`，可部署到任意静态网站托管服务。

## 更新行程

直接编辑根目录的 `trip-template.md`。保存后，开发服务器会自动刷新页面。

路线目前是目的地之间的示意连线，不是实际驾车导航路线。
